{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "detail": "build the iso image for the homelab machine(s)",
      "command": "docker build --iidfile build.imageid . && mkdir -p build && docker run --rm $(cat build.imageid && rm build.imageid) > build/homelab.iso"
    }
  ]
}